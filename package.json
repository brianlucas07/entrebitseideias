{
  "name": "blog-entrebitseideias",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "build:posts": "node scripts/build-posts.js",
    "build": "node scripts/build-posts.js && tsc",
    "watch": "tsc --watch",
    "dev": "node scripts/build-posts.js && tsc --watch"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  },
  "dependencies": {
    "marked": "^18.0.12"
  }
}
